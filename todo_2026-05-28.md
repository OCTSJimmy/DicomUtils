# NeedList 受试者白名单 — 设计总结与待办

> 时间：2026-05-28
> 状态：设计已冻结，待编码

---

## 一、需求定义

Need 列表 = **部分受试者白名单**。脱敏表含全部受试者，某次运行只需处理其中一部分时，用 Need 列表做过滤。

**约束**：
1. Need 列表里的编号有两种形式：a) 原始受试者编号（SubjectNumber，含分中心前缀）b) 虚拟受试者编号（vSubjectNumber，含分中心前缀）
2. 受试者编号只能从目录结构中提取，正则固定（`ORIGIN_SUBJECT_CODE_REPLACE_REGEX`）
3. Need 列表是静态 CSV 文件，单列表头

---

## 二、核心设计：正交过滤

**原则：初始化阶段做交集，扫描阶段零感知。**

```
[CodeManager.init()]        加载全部脱敏表 (All)
        ↓
[NeedListManager.createList()]  读取 Need CSV → 编号列表
        ↓
[CodeManager.buildEffective()]  All ∩ Need = effectiveSubjects Map
        ↓
[SedaScanner]               统一查 effective Map，命中即处理，未命中即 Ignore
```

无 Need 或 Need 无效时，`effective` 直接引用 `All` 的完整 Map。扫描层代码路径唯一，无需分支。

---

## 三、数据格式

### Need CSV（单列，有表头）

**NEED_ID_MODE = original**：
```csv
SubjectNumber
001-01001
001-01002
```

**NEED_ID_MODE = virtual**：
```csv
vSubjectNumber
C001-P01523
C001-P01524
```

- 仅第一列有效，表头名不限
- 编码：UTF-8（与脱敏表一致）

---

## 四、配置项

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `NEED_SUBJECT_LIST_CSV_PATH` | String | `""` | Need 白名单 CSV 路径，空则全量扫描 |
| `NEED_ID_MODE` | Enum | `original` | 编号模式：`original`（原始编号）或 `virtual`（虚拟编号） |

---

## 五、统计口径变更

| 场景 | 旧口径 | 新口径 |
|------|--------|--------|
| 编号不在 All 中 | Error | Error（数据错误） |
| 编号在 All 中但不在 Need 中 | — | **Ignore**（配置过滤，非错误） |

---

## 六、已完成项

1. [x] `Settings.kt` 新增 `NEED_ID_MODE` 配置读取
2. [x] `Design.md` 新增 NeedListManager 数据模型、配置项、正交设计决策章节
3. [x] `README.md` 新增白名单特性、配置示例、CSV 格式说明、目录结构

---

## 七、待办项（编码环节）

1. [ ] 改造 `CodeManager`
   - 新增 `mEffectiveCodeModuleMap`
   - `init()` 加载 All 后，调用 `buildEffective(needList)` 取交集
   - `operator get()` 统一查 effective Map（无 Need 时指向 All）

2. [ ] 实现 `NeedListManager`
   - `createList()`：读取 CSV，按 `NEED_ID_MODE` 解析第一列，返回 `List<String>`
   - 空文件/路径不存在时返回空列表

3. [ ] 修改 `SedaScanner.inspectAndDispatch()`
   - `codeModule == null` 时统计改为 `fileIgnored / subjectIgnored`（确认此分支是 Need 过滤还是数据缺失）
   - 若 `CodeManager.get()` 因 Need 过滤返回 null，走 Ignore 分支
   - 若因 All 中不存在返回 null，走 Error 分支
   - **待决策**：CodeManager 是否需要区分这两种 null

4. [ ] 日志输出
   - `buildEffective` 完成后输出 `Need 加载：X 条，有效：Y 条，无效：Z 条`
   - 无效的 Need 编号单独列一行 warn，便于排查数据不同步

5. [ ] 测试验证
   - 无 Need 时全量通过
   - Need 为 original 模式时仅匹配原始编号
   - Need 为 virtual 模式时仅匹配虚拟编号
   - Need 含无效编号时 warn 日志正确输出
   - 扫描统计 Ignore / Error 分流正确

---

## 八、接口草案

```kotlin
// NeedListManager.kt
object NeedListManager {
    fun createList(csvPath: String = Settings.NEED_SUBJECT_LIST_CSV_PATH): List<String>
}

// CodeManager.kt
enum class CodeManager {
    INSTANCE;
    
    private val mAllOriginMap = HashMap<String, CodeModule>()
    private val mAllDesensitizedMap = HashMap<String, CodeModule>()
    private var mEffectiveMap: Map<String, CodeModule> = emptyMap()
    
    fun init(csvPath: String, needList: List<String> = emptyList())
    fun buildEffective(needList: List<String>)
    operator fun get(key: String): CodeModule? = mEffectiveMap[key]
}
```

---

## 九、搁置说明

设计已冻结。待用户触发编码指令后，按上表逐项实施。编码前需确认：
- `CodeManager.get()` 是否需要区分 "Need 过滤 null" vs "All 中不存在 null"
- `NeedListManager` 作为独立单例还是并入 `CodeManager`

